@page "/profile"
@using GreenLebanon.Taxi.Web.Identity
@inject NavigationManager Navigation
@inject IAccountManagement AccountManagement 
<div class="container d-flex justify-content-center align-items-center vh-100 bg-white">
    <div class="card shadow-lg" style="width: 22rem; border-radius: 1rem;">
        <div class="card-body text-center">
            <img src="@ProfileImage" alt="Profile Picture" class="rounded-circle border border-info mb-3" style="width: 120px; height: 120px;" />
            <h2 class="card-title mb-2">@Name</h2>
            <p class="card-text text-muted mb-1"><i class="bi bi-envelope"></i> @Email</p>
            <p class="card-text text-muted"><i class="bi bi-telephone"></i> @PhoneNumber</p>
            
            <button class="btn btn-success mt-3" @onclick="EditProfile">Edit Profile</button>
            <button class=" btn btn-danger mt-3" @onclick="Logout" > Logout </button>
        </div>
    </div>
</div>


<a href="client-history"></a>
@code {
    // Example profile data
    private string ProfileImage = "favicon.png"; 
    private string Name = "Mhmd Ali";
    private string Email = "mhmdali@gmail";
    private string PhoneNumber = "76896326";


    private void EditProfile() {
        // Logic to handle profile editing
        // You can add modal functionality here to open an editing form
    }
    private async Task Logout() {
        //await AccountManagement.LogoutAsync();
        Navigation.NavigateTo("/login");
    }
}
